C2 "co-op", decided by Ben 2026-09-23 right after the game was published (both place ids live).
Contract: `docs/INTERFACES.md` "C2 contracts — Co-op".

- **Boss HP scales with party size** (`coop.bossHpPerExtraPlayer`, pure `Combat.WaveSpec` →
  `bossHpMult`, mirrored in `sim_combat.py`). Regular enemies still scale by count only.
- **Run list = this hub server + friends + public runs from any server.** Host toggle "Open to
  public" (default `coop.publicByDefault`); private runs only to friends/same-server.
- **Hub party is invite + accept**, same server, leader picks the mission, one reserved server
  and ONE group `TeleportAsync`. Party lives in memory; re-forms after the group return only
  among members whose own teleport hints agree (a forged hint cannot pull anyone in).
- **Missions are host-gated**: guests may fight any mission the host unlocked. Enforced through
  the server-only `RunTicket` (ActiveRunsCodes hash map) whose `privateServerId` must equal
  `game.PrivateServerId` — client teleport data is forgeable and never grants access.
- Verification is **both** Studio (bridge + levers `fakeParty`, `fakeInvite`, `admitCheck`, bots)
  and a published two-account round trip.
- Lead deduped the combat client UI copies by mapping the hub files in `combat.project.json`.
  Rojo does NOT merge an explicit child into a `$path` directory of the same name — it makes two
  instances — so the combat `Client` node lists `Main`, `UI`, `Controllers` explicitly.
- C2 ran in worktree `C:\Users\benja\Desktop\tycoon-c2` (branch `c2-coop`) while another session
  did M9 wave 2d in the main checkout (Types.luau + INTERFACES.md both touched: merge, don't overwrite).

**Why:** a fresh session would otherwise re-open these rulings or trust teleport data for access.

**How to apply:** read the C2 contract before touching party/registry/admission code. See
[[combat-2026-09-17]], [[studio-bridge-2026-09-22]], [[audience-reach-2026-09-18]].
